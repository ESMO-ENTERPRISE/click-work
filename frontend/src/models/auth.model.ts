export type SignIn = {
    email: string
    password: string
}

export type SignUp = {
    name: string
    lastName: string
    email: string
    password: string
    username: string
    role: "USER"
}